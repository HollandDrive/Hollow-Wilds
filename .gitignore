# Unreal Engine generated / transient folders
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
.vs/
*.VC.db
*.opensdf
*.opendb
*.sdf
*.sln
*.slnx
*.suo
*.xcodeproj
*.xcworkspace

# Build receipts / generated
*.target
Source/**/*.generated.h

# OS noise
Thumbs.db
.DS_Store

# Keep these:
!Config/
!Content/
!Source/
!*.uproject
!Docs/

# Synty (and other marketplace) art packs: large + licensed — keep out of git.
# Migrated into the project on disk; not redistributable via the repo.
Content/Biomes/
